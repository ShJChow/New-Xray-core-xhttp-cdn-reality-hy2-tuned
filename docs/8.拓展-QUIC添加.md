# XHTTP + CDN 搭建指南 (八)：拓展 - QUIC 添加

> **重要勘误（v4.4.1，2026-08-06 实机故障）**：本文第 4 节原先教的做法是
> 「往 CDN / Reality 域名**已有的** `server` 块里插 `listen ... quic`」。
> 该做法在实机上会**打死默认的 `Vless-xhttp-tls-UDP-cdn` 节点**——nginx 持续报
> `upstream rejected request with error 5 ... upstream: "grpc://127.0.0.1:8001"`
> （gRPC error 5 = NOT_FOUND，Xray 8001 在拒绝上行 POST），三条 h3 节点连同
> 默认 CDN 节点一起不通。删掉插入的两行并重启 nginx 后主链路立刻恢复。
>
> **机制未查清。** 能排除的是 `add_header`（只作用于响应，而故障发生在读上游
> 响应头之前的请求阶段）；嫌疑落在 `listen ... quic reuseport` 上，但「一个 UDP
> 监听为何会打死同块 TCP 8003 的请求路径」这条因果链尚未推出。
>
> `extensions/quic-h3` 已改为**追加独立 `server` 块**、不触碰任何既有块，
> 并新增了一条「装完之后主链路指纹是否变化」的事后校验（变了就自动回滚）。
> 默认端口也由 UDP 443 改为 UDP 8445。下文第 4 节的配置片段已同步更新。
> 手工配置的读者请照新片段做，**不要**再往既有 server 块里插 listen。

> **勘误（v1.2.7）**：本拓展产出的 `Vless-xhttp-tls-h3-direct` 节点，用户在 **Shadowrocket 下实测不通**，与主脚本的 `Vless-xhttp-tls-UDP-direct` 表现一致（主脚本那条已在 v2.0.0 移除）。
>
> 本拓展**保留不动**，因为它同时产出 `Hysteria2-direct` 节点——那条走独立协议，与 h3 无关。**若你只想要 Hysteria2，运行本拓展即可，直接忽略 h3 那条节点。**
>
> v1.2.1 修复的是服务端侧的 SNI ↔ `server_name` 错配；客户端侧为何仍不通尚未定位（缺少可复现的抓包）。

在已有配置上增加：

```text
VLESS + XHTTP + TLS + H3
Hysteria2
```

H2 直连不单独增加节点。将现有 `xhttp+TLS 双向 CDN` 节点的连接地址改成 VPS IP，同时保留 TLS、SNI、Host 和 `alpn: h2`，就是 XHTTP H2/TCP 直连。

XHTTP H3 直连节点使用 **Reality 域名**（Cloudflare 灰云、DNS 直指 VPS）作为 SNI 和 Host —— 本拓展的 QUIC 监听与 XHTTP `location` 都挂在 Reality 域名的 Nginx `server` 块内，SNI 必须与之一致，否则 TLS 握手会落到回落网站上，节点必然不通（v1.2.1 修复）。连接地址填写 VPS IP。

ECH 是 Cloudflare CDN 侧的机制，直连节点不适用，v1.2.1 起不再为该节点生成 ECH 参数。

## 1. 工作方式

默认使用分开端口：

```text
XHTTP H3：客户端 → VPS UDP 443 → Nginx → Xray 127.0.0.1:8001
Hysteria2：客户端 → VPS UDP 8443 → Hysteria2
```

也可以在拓展脚本中选择共用端口：

```text
XHTTP H3：客户端 → VPS UDP 443 → Hysteria2 masquerade → Nginx 127.0.0.1:8003 → Xray 127.0.0.1:8001
Hysteria2：客户端 → VPS UDP 443 → Hysteria2
```

共用端口模式下，XHTTP H3 的公网 QUIC/TLS 由 Hysteria2 处理。

## 2. DNS 与防火墙

Reality 域名保持仅 DNS（灰色云朵），解析到 VPS。

默认放行：

```text
UDP 443
UDP 8443
```

修改默认端口后，放行实际使用的 UDP 端口。

## 3. 证书

两个节点复用：

```text
/etc/ssl/private/fullchain.cer
/etc/ssl/private/private.key
```

检查证书：

```bash
openssl x509 -in /etc/ssl/private/fullchain.cer -noout -ext subjectAltName
```

## 4. VLESS + XHTTP + TLS + H3 服务端

XHTTP H3 复用 `/usr/local/etc/xray/config.json` 中已有的 `127.0.0.1:8001` XHTTP 入站，不增加新的 Xray 入站。公网 HTTP/3 由 Nginx 接收。

编辑 `/etc/nginx/nginx.conf`，在 `http { ... }` 的**末尾追加一个独立 server 块**。
注意：**不要**往已有的 CDN / Reality server 块里插 `listen ... quic`，原因见文首勘误。

```nginx
server {
    listen 8445 quic reuseport;
    server_name your-cdn-domain;

    ssl_certificate /etc/ssl/private/fullchain.cer;
    ssl_certificate_key /etc/ssl/private/private.key;

    location /your-existing-xhttp-path {
        grpc_pass 127.0.0.1:8001;
        grpc_socket_keepalive on;
        grpc_read_timeout     1h;
        grpc_send_timeout     1h;
        grpc_connect_timeout  15s;
        grpc_set_header Host                  $host;
        grpc_set_header X-Real-IP             $real_client_ip;
        grpc_set_header Forwarded             $proxy_add_forwarded;
        grpc_set_header X-Forwarded-For       $proxy_add_x_forwarded_for;
        grpc_set_header X-Forwarded-Proto     $scheme;
    }

    location / {
        return 404;
    }
}
```

将 `/your-existing-xhttp-path` 改成 `/usr/local/etc/xray/config.json` 中 `127.0.0.1:8001` 入站使用的 XHTTP Path，`your-cdn-domain` 改成你的 CDN 域名（客户端节点的 `sni` / `host` 必须与之字面相等）。既有的 CDN / Reality server 块**保持原样不动**。

改完之后，除了 `nginx -t` 和 `systemctl status nginx`，务必再验一次**默认 CDN 节点还通不通**——那次故障里前两项全部通过，主链路却已经死了。

## 5. Hysteria2 服务端

### Debian / Ubuntu

```bash
HYSTERIA_USER=root bash <(curl -fsSL https://get.hy2.sh/)
```

### Alpine Linux

`x86_64`：

```sh
curl -fL https://github.com/apernet/hysteria/releases/latest/download/hysteria-linux-amd64 -o /usr/local/bin/hysteria
chmod +x /usr/local/bin/hysteria
```

`aarch64`：

```sh
curl -fL https://github.com/apernet/hysteria/releases/latest/download/hysteria-linux-arm64 -o /usr/local/bin/hysteria
chmod +x /usr/local/bin/hysteria
```

创建 `/etc/init.d/hysteria-server`：

```sh
cat > /etc/init.d/hysteria-server <<'EOF'
#!/sbin/openrc-run

name="hysteria-server"
description="Hysteria2 Server"
command="/usr/local/bin/hysteria"
command_args="server --config /etc/hysteria/config.yaml"
command_background=true
pidfile="/run/hysteria-server.pid"
output_log="/var/log/hysteria-server.log"
error_log="/var/log/hysteria-server.log"

depend() {
    need net
}
EOF

chmod +x /etc/init.d/hysteria-server
rc-update add hysteria-server default
```

创建配置目录并编辑 `/etc/hysteria/config.yaml`：

```bash
mkdir -p /etc/hysteria
vi /etc/hysteria/config.yaml
```

Hysteria2 默认使用 UDP 8443：

```yaml
listen: :8443

tls:
  cert: /etc/ssl/private/fullchain.cer
  key: /etc/ssl/private/private.key

auth:
  type: password
  password: YOUR_HY2_PASSWORD

masquerade:
  type: proxy
  proxy:
    url: https://127.0.0.1:8003
    rewriteHost: false
    insecure: true
```

Hysteria2 的普通 HTTP/3 请求反向代理到 Nginx，由 Reality 域名的 `location /` 返回反向代理网站或本地静态页面。选择共用端口模式时，将 `listen` 改成 XHTTP H3 使用的端口，并删除 Nginx 中的 `listen ... quic` 和 `Alt-Svc`。

## 6. Mihomo 客户端配置

文件：

```text
/home/USERNAME/client-config-mihomo-full.yaml
/home/USERNAME/client-config-mihomo-nodes.yaml
```

```yaml
  - name: vless+xhttp+tls+h3 直连
    type: vless
    server: VPS_IP
    port: 443
    uuid: YOUR_UUID2
    udp: true
    flow: ""
    tls: true
    encryption: "YOUR_VLESSENC_ENCRYPTION"
    network: xhttp
    alpn:
      - h3
    servername: cdn.example.com
    client-fingerprint: chrome
    xhttp-opts:
      host: cdn.example.com
      path: /your-existing-xhttp-path
      mode: auto

  - name: hysteria2 直连
    type: hysteria2
    server: VPS_IP
    port: 8443
    password: "YOUR_HY2_PASSWORD"
    sni: reality.example.com
    alpn:
      - h3
```

## 7. v2rayN / Shadowrocket 客户端配置

文件：

```text
/home/USERNAME/client-config.txt
```

XHTTP H3：

```text
vless://YOUR_UUID2@VPS_IP:443?encryption=YOUR_VLESSENC_ENCRYPTION&security=tls&sni=cdn.example.com&fp=chrome&alpn=h3&insecure=0&allowInsecure=0&type=xhttp&host=cdn.example.com&path=%2Fyour-existing-xhttp-path&mode=auto#vless%2Bxhttp%2Btls%2Bh3%20%E7%9B%B4%E8%BF%9E
```

Hysteria2：

```text
hysteria2://YOUR_HY2_PASSWORD@VPS_IP:8443/?sni=reality.example.com&insecure=0#hysteria2%20%E7%9B%B4%E8%BF%9E
```

## 8. 检查与重启

检查配置：

```bash
nginx -t
xray -test -config /usr/local/etc/xray/config.json
```

Debian / Ubuntu：

```bash
systemctl enable --now hysteria-server.service
systemctl restart nginx
ss -lnup | grep -E ':(443|8443)\b'
```

Alpine Linux：

```sh
rc-service hysteria-server restart
rc-service nginx restart
ss -lnup | grep -E ':(443|8443)\b'
```
