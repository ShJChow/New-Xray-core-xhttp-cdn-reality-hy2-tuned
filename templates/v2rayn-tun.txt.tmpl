==================================================
v2rayN TUN 模式配置清单（本机专用，安装时按实际值生成）
==================================================

为什么需要这份清单
--------------------------------------------------
TUN 用 auto-route 把默认路由指向虚拟网卡。客户端**自己发往节点服务器**的包
会被自家 TUN 再次捕获，按路由规则兜到 MATCH → 又送回代理 → 环路。
表现是：开 TUN 后节点全部超时，关掉 TUN 立刻正常。

这不是服务端问题，节点 URI 里也没有任何参数能修它——必须在客户端把
**服务器自身的地址**排除出 TUN 的接管范围。以下三个地址就是本机的全部出口。


必须设为「直连 / 绕过」的地址
--------------------------------------------------
  节点服务器 IP     ${VPS_IP}
  CDN 域名          ${CDN_DOMAIN}
  Reality 域名      ${REALITY_DOMAIN}

其中 IP 是最关键的一条：节点 1/2 直接连它，v2rayN 只要为它加一条直连路由，
这两条节点在 TUN 下就不会自环。
节点 3 走 CDN 域名，域名要先解析——所以域名也必须在直连列表里，
否则解析请求本身会进 TUN。


v2rayN 里的设置位置
--------------------------------------------------
0) **开 TUN 后所有节点都不通，先勾这一项**：
   参数设置 → 勾选「旧版Tun保护」（英文 legacy process name tun protect）。

   v2rayN v7.19+ 换了 TUN 保护的实现——用一个 Shadowsocks 中继 + 端口路由来防
   回环，但这套新机制在**部分 Windows 系统上直接失效**，症状就是「开 TUN 全不通、
   关掉立刻正常」。旧实现靠进程名识别把内核进程排除出 TUN 拦截，跨平台可靠性差
   一些，但在新机制失效的机器上能工作。见 2dust/v2rayN PR #9005。

   这一条与下面的直连规则是**两个独立问题**：本项属于「TUN 保护机制本身失效」，
   下面属于「节点自身流量被 TUN 捞走」。全不通先看这条，个别节点不通看下面。

1) 路由设置 → 新增规则 → outboundTag 选 direct，把上面三个地址填进
   「域名」和「IP」两栏（IP 填 ${VPS_IP}，域名填两个）。
   规则要排在其它规则**之前**，否则可能被前面的规则抢先命中。

2) 参数设置 → TUN 设置 → 确认「严格路由 / strict route」已开启。
   关闭时部分平台兜不住 UDP，QUIC 节点最先出问题。

3) 如果用订阅：更新订阅不会覆盖路由设置，规则加一次即可长期生效。

（菜单文字随 v2rayN 版本略有差异，按「路由」「TUN」「旧版」三个关键词找即可。）


节点能开网页、但测速显示 -1
--------------------------------------------------
**能开网页就说明节点是通的**，-1 是测试本身失败，不等于节点不可用。
两种测试走的是完全不同的路径，所以失败原因也不同：

  Tcping / 测速(Tcping)
    从本机**直连** 节点服务器:端口，**不经过代理**。
    TUN 接管默认路由后，这个直连请求会被自家 TUN 捞走 → 送回代理 → 回环 → 超时。
    而浏览器流量是正常经代理出去的，所以「能上网但 Tcping -1」。
    → 修法就是上面第 1 项的直连规则。

  真连接延迟
    **经代理**请求 v2rayN 内置的测试 URL，默认第一条是
    https://www.google.com/generate_204（见 v2rayN 源码 Global.cs 的
    SpeedPingTestUrls）。两种失败原因：
      a) 该 URL 从落地不可达 → 换成列表里的
         http://www.msftconnecttest.com/connecttest.txt 或
         https://www.apple.com/library/test/success.html
      b) QUIC + XHTTP 冷启动超过测试超时 → 浏览器能用是因为复用了已经热起来的
         会话，而测试每次都是冷启动。这种情况可以直接忽略。

另外：**对 alpn=h3 的节点用 Tcping 本身语义就是错的**——Tcping 做 TCP 握手，
而该节点跑在 UDP 443 上。测出来的数字即使不是 -1，也不代表它的真实延迟。


本机各节点在 TUN 下的稳健度排序
--------------------------------------------------
依据是本项目的实测记录，不是推测：

  最稳  1. Vless-reality-vision              裸 IP + TCP
        2. Vless-xhttp-reality               裸 IP + TCP
        3. Vless-xhttp-h2-tcp-direct         裸 IP + TCP ${H2_PORT}
        4. Hysteria2-obfs                    裸 IP + UDP ${HY2_PORT}
        5. Vless-xhttp-h3-direct             裸 IP + UDP ${H3_PORT}
  最弱  6. Vless-xhttp-tls-UDP-cdn           域名 + QUIC/h3

注意「最弱」指的是**对 TUN 配置的敏感度**，不是速度。节点 6 实测是最快的一条
（iOS onexray 下快于 Hysteria2），只是它经 CDN、server 是域名，必须把上面的
CDN 域名加进直连列表才能在 TUN 下正常工作。配好了它就是首选。

节点 3 是直连 VPS 裸 IP 的 TCP 节点（v4.7.0 新增），传输层与节点 1/2 同属 TCP，
在 TUN 下同样稳健，且不受运营商封 UDP 影响——节点 5 连不上时优先试它。
节点 4/5 是直连 VPS 裸 IP 的 UDP 节点，只要为 ${VPS_IP} 加了直连路由就不会自环，
比节点 6 少一层域名解析的依赖。三者都需要在**云厂商安全组**里放行对应端口
（UDP ${H3_PORT}、UDP ${HY2_PORT}、TCP ${H2_PORT}）——这一层在机器外面，脚本查不到也改不了。

TCP 之所以更稳，是因为 dialer 绑定物理接口的保护在 TCP 路径上更完整；
QUIC 是无连接 UDP，同样的保护在多数平台上兜不住。

若照上面配好直连规则后节点 5 仍不通，**先换节点 1 或 2 验证**：
1/2 能通而 3/4/5 都不通 = 客户端 UDP 侧或安全组的问题，与 Xray 配置无关。


mihomo / Clash 用户不需要这份清单
--------------------------------------------------
本项目生成的 client-config-mihomo-full.yaml 已内置三道防线：
  tun.route-exclude-address   让节点流量根本不进 TUN
  rules 首条 DIRECT           万一进了 TUN 也第一条放出去
  sniffer.skip-dst-address    即使被捞到也不许改写目标地址
直接用那份配置即可，无需手工添加规则。
