==================================================
v2rayN TUN 模式配置清单（本机专用，安装时按实际值生成）
==================================================

为什么需要这份清单
--------------------------------------------------
TUN 用 auto-route 把默认路由指向虚拟网卡。客户端**自己发往节点服务器**的包
会被自家 TUN 再次捕获，按路由规则兜到 MATCH → 又送回代理 → 环路。
表现是：开 TUN 后节点全部超时，关掉 TUN 立刻正常。

这不是服务端问题，节点 URI 里也没有任何参数能修它——必须在客户端把
**服务器自身的地址**排除出 TUN 的接管范围。以下三个地址就是本机的全部出口。


必须设为「直连 / 绕过」的地址
--------------------------------------------------
  节点服务器 IP     ${VPS_IP}
  CDN 域名          ${CDN_DOMAIN}
  Reality 域名      ${REALITY_DOMAIN}

其中 IP 是最关键的一条：节点 1/2/5 直接连它，v2rayN 只要为它加一条直连路由，
这三条节点在 TUN 下就不会自环。
节点 3/4 走 CDN 域名，域名要先解析——所以域名也必须在直连列表里，
否则解析请求本身会进 TUN。


v2rayN 里的设置位置
--------------------------------------------------
1) 路由设置 → 新增规则 → outboundTag 选 direct，把上面三个地址填进
   「域名」和「IP」两栏（IP 填 ${VPS_IP}，域名填两个）。
   规则要排在其它规则**之前**，否则可能被前面的规则抢先命中。

2) 参数设置 → TUN 设置 → 确认「严格路由 / strict route」已开启。
   关闭时部分平台兜不住 UDP，QUIC 节点最先出问题。

3) 如果用订阅：更新订阅不会覆盖路由设置，规则加一次即可长期生效。

（菜单文字随 v2rayN 版本略有差异，按「路由」「TUN」两个关键词找即可。）


本机各节点在 TUN 下的稳健度排序
--------------------------------------------------
依据是本项目实测记录（tasks/lessons.md L15），不是推测：

  最稳  1. Vless-reality-vision              裸 IP + TCP
        2. Vless-xhttp-reality               裸 IP + TCP
        5. Vless-xhttp-split-realityup-cdndown  上行裸 IP，下行走域名
        3. Vless-xhttp-split-cdnup-realitydown  上行走域名
  最弱  4. Vless-xhttp-tls-UDP-cdn           域名 + QUIC/h3

TCP 之所以更稳，是因为 dialer 绑定物理接口的保护在 TCP 路径上更完整；
QUIC 是无连接 UDP，同样的保护在多数平台上兜不住。

若照上面配好直连规则后节点 4 仍不通，**先换节点 1 或 2 验证**：
1/2 能通而 4 不通 = 客户端 UDP 侧的问题，与服务端无关。


mihomo / Clash 用户不需要这份清单
--------------------------------------------------
本项目生成的 client-config-mihomo-full.yaml 已内置三道防线：
  tun.route-exclude-address   让节点流量根本不进 TUN
  rules 首条 DIRECT           万一进了 TUN 也第一条放出去
  sniffer.skip-dst-address    即使被捞到也不许改写目标地址
直接用那份配置即可，无需手工添加规则。
